server:
  port: 8000

spring:
  datasource:
    url: jdbc:postgresql://task_manager_db:5433/EffectiveMobileTaskManager
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret-access-key: fgsdffsg456456rertr66yerty56465sdfmk68poiopz