server:
  port: 8080

spring:
  datasource:
    url: ${DB_DRIVER}://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret-access-key: fgsdffsg456456rertr66yerty56465sdfmk68poiopz
  secret-refresh-key: sssdffsg456456rertr66yerty589898ghtrcop9741b
  access-token-lifetime: 86_400_000 # Millis
  refresh-token-lifetime: 604_800_000 # Millis
